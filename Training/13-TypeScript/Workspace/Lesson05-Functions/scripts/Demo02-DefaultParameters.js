function show(num1, num2, num3) {
    if (num3 === void 0) { num3 = 200; }
    console.log(num1 + ", " + num2 + ", " + num3);
}
show(100, 200);
//Default parameters will work for final parameter like in middel we can levae for default(100,,200)
